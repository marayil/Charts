import { TextManipulationPlugin } from './text-manipulation-plugin';

describe('TextManipulationPlugin', () => {
  it('should create an instance', () => {
    expect(new TextManipulationPlugin()).toBeTruthy();
  });
});
